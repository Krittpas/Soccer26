#include <POP32.h>  
void setup() 
{
  oled.text(0,0,"Hello World"); // แสดงข้อความ Hello World ที่ตำแหน่งซ้ายสุดของบรรแรกสุด(บรรทัดที่1)
  oled.show();                  // ปรับปรุงการแสดงผล
}
void loop() 
{}
